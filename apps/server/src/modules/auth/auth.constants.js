const ROLES = Object.freeze({
  ADMIN: 'ADMIN',
  CREATOR: 'CREATOR',
  LEARNER: 'LEARNER',
});

const PUBLIC_REGISTRATION_ROLES = [ROLES.CREATOR, ROLES.LEARNER];

module.exports = { ROLES, PUBLIC_REGISTRATION_ROLES };
