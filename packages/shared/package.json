{
  "name": "@learndojoworld/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "echo \"No build step required for shared\"",
    "lint": "eslint src --ext .js",
    "format": "prettier --write src",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "jest": "^29.6.0",
    "@types/jest": "^29.5.3",
    "prettier": "^3.0.0"
  }
}